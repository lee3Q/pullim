# Handoff — 2026-03-29_11

## 이번 세션 요약 (3줄 이내)
반자동화 파이프라인 2단계 하네스 실행 — 텔레그램 봇 PM2 설정 + startup 등록 완료.
`notify-telegram.sh` Markdown 파싱 에러(핸드오프_10 미결 이슈) fix 완료.
모든 AC 통과 + Next.js 빌드 확인.

## 핵심 맥락 (다음 세션이 반드시 알아야 할 것)
- pullim-bot 이미 PM2 online 상태(6h uptime)였음 — 새로 시작한 게 아니라 기존 프로세스 유지
- `notify-telegram.sh` 버그: `-d text="$MSG" -d parse_mode="Markdown"` 조합이 특수문자(_*[) 포함 메시지에서 Telegram API 파싱 에러 발생 → `--data-urlencode` + parse_mode 제거로 fix
- PM2 launchd startup 등록 완료: `/Users/sanggyulee/Library/LaunchAgents/pm2.sanggyulee.plist` 생성. `sudo` 실행 시 LaunchAgents 경로 warning이 나오지만 실제 등록은 성공
- morning-report.sh / evening-report.sh 모두 존재 확인됨 (하네스에서 "없으면 생성" 조건 → 미해당)
- night-batch.sh의 텔레그램 알림은 notify-telegram.sh를 거치지 않고 curl 직접 호출 — 영향 없음

## 진행 중인 작업
- [x] 반자동화 파이프라인 2단계 하네스 완료 (AC 전부 통과)
- [ ] 3단계(아침/밤 리포트 launchd 자동 실행 스케줄) — 이 하네스 범위 밖, 다음 작업

## 보류/미결 사항
- 없음 (하네스 범위 내 전부 완료)

## 참고 파일
- `.state/harnesses/2026-03-29_반자동화_텔레그램봇.md` — 실행한 하네스
- `scripts/telegram-bot.js` — 봇 소스 (310줄, 변경 없음)
- `scripts/ecosystem.config.js` — PM2 설정 (변경 없음, 기존 파일 사용)
- `scripts/notify-telegram.sh` — **수정됨** (Markdown 파싱 에러 fix)
- `scripts/morning-report.sh` — 확인 (변경 없음)
- `scripts/evening-report.sh` — 확인 (변경 없음)
- `scripts/night-batch.sh` — 확인 (변경 없음)
