# Handoff — 2026-03-29

## 이번 세션 요약 (3줄 이내)
아포칼립스 테마 이미지 코드 연결 하네스 실행. 3곳(themes avatar/bg, 홈카드 imagePath) 수정 + CC 교차검증 → avatar dead code·per-scene bg 구조 미지원 발견 → SceneConfig bgImage 필드 추가 + ThemedBackground 종말 전용 브랜치로 13개 씬별 bg 완전 연결. 빌드 통과, 커밋 푸시 완료 (b1bb8f7).

## 핵심 맥락 (다음 세션이 반드시 알아야 할 것)
- `assets.avatar`는 어떤 세션 컴포넌트도 읽지 않음 → 경로는 설정됐지만 화면에 표시 안 됨. 아바타 표시가 필요하면 별도 컴포넌트 작업 필요
- `SceneConfig.bgImage`는 이번에 종말 테마에만 값이 있음. 다른 테마(모험가, 전략실, 달빛정원 등)는 bgImage 없음 — 필요 시 같은 패턴으로 추가 가능
- ThemedBackground.tsx의 종말 브랜치는 씬별 bgImage 있으면 교체, 없으면 `theme.assets.bg`(apocalypse-enter-bg) fallback

## 진행 중인 작업
- [x] 하네스 2026-03-29_이미지코드적용.md — 전 항목 완료

## 보류/미결 사항
- avatar 실제 표시: 세션 컴포넌트 어디에 표시할지 설계 없음. 필요 여부 결정 후 진행
- 다른 테마(stargazer 등) 씬별 bg 확장: 구조는 준비됨, 이미지만 있으면 같은 패턴 적용 가능

## 참고 파일
- `pullim/src/lib/themes/index.ts` — SceneConfig bgImage 추가, 종말 assets/scenes 수정
- `pullim/src/components/ultimate/backgrounds/ThemedBackground.tsx` — 종말 전용 브랜치 추가
- `pullim/src/app/page.tsx` — apocalypse 홈카드 imagePath 수정
- `.state/harnesses/2026-03-29_이미지코드적용.md` — 이번 세션 하네스
