# 풀림 (Pullim)

AI 의사결정/멘탈케어 SaaS. 1인 창업 (대표: 이상규).

## 세션 시작

1. `.state/project-config.md` 읽기 (불변 설정)
2. `.state/projects/` 내 active 프로젝트 파일 읽기 (복수 가능)
3. 필요 시 `.state/decisions/` 최근 결정, `.state/handoffs/` 최근 핸드오프 확인

## 세션 종료

1. 작업한 프로젝트 파일 갱신 (`.state/projects/프로젝트명.md`)
2. 프로젝트 완료 시 → `.state/projects/_done/` 으로 이동
3. 주요 결정이 있었으면 `.state/decisions/YYYY-MM-DD_제목.md` 추가 (덮어쓰기 금지)
4. `/handoff` 실행 권장

## 핵심 자료

| 키워드 | 경로 |
|--------|------|
| 웹앱 소스 | ./pullim/ |
| PRD, 제품 스펙 | ./제품/ |
| 리서치 | ./리서치/ |
| 사전 (풀림 용어) | ./사전/INDEX.md |
| 신청서, PPT | ./신청서/ |
| 테스트 세션 기록 | ./테스트/ |
| 아카이브 (이전 팀 자료) | ./archive/ |

## 원칙

- AI 팀 시뮬레이션 금지 (구조적으로 실패 — 리서치 검증 완료)
- 의사결정 시 교차 모델 검증 (최소 2개 이종 모델)
- **교차검증 실행은 Claude가 직접**: `gclaude -p "프롬프트"` 로 GLM-5를 CLI에서 직접 실행. 대표에게 프롬프트를 넘기지 않는다.
- 덮어쓰기 금지: decisions/, sessions/ 는 추가만
- 위기 안전: RED → LLM 즉시 중단 → 전문기관 안내

## 워크플로우 강제 규칙

**프로젝트 파일의 "단계"를 반드시 따른다.**
- 각 프로젝트 파일(.state/projects/)에 단계가 명시되어 있음
- 현재 단계의 방법(리서치 방식, 검증 방식 등)을 반드시 준수
- 단계를 건너뛰거나 방법을 바꿔서 실행하지 않는다

**더 효율적인 방법이 있다고 판단되면:**
- 실행하지 말고 **제안**한다 ("이렇게 하면 더 효율적일 것 같은데, 변경할까요?")
- 대표가 승인하면 프로젝트 파일의 단계를 갱신한 후 실행
- 대표가 거부하면 기존 단계대로 실행

**응답 순서 규칙 (강제):**
- 대표에게 넘길 작업(리서치 프롬프트, 다른 세션용 프롬프트, 확인 질문 등)이 있으면 **반드시 먼저 출력**
- 그 다음에 혼자 할 수 있는 작업(코드, 문서 작성 등)을 이어서 진행
- "프롬프트 드릴까요?" 라고 물어보지 말고 바로 줘. 대표가 기다리는 시간을 최소화할 것
- 나쁜 예: [긴 작업 수행] → "프롬프트 드릴까요?"
- 좋은 예: [프롬프트 먼저 출력] → [긴 작업 수행]

**리서치 워크플로우:**
- Claude 단독 리서치 금지
- 최소: 대표에게 리서치 프롬프트를 먼저 출력 → 대표가 병렬 진행하는 동안 Claude는 다른 작업
- project-config.md의 워크플로우 스택 참조

**의사결정 검증 워크플로우 (강제):**
- 아래 상황에서는 반드시 /cross-check 또는 교차 검증을 제안해야 함:
  1. 비용/수치 결정 (가격, BEP, 마진, API 비용 등 숫자가 확정되는 순간)
  2. 기술 스택 선택 (모델 선택, 도구 선택, 아키텍처 결정)
  3. 전략 방향 전환 (피벗, 포지셔닝 변경, 타겟 변경)
  4. 외부 제출물 확정 (사업계획서, 발표대본 등 외부에 나가는 문서)
- 검증 없이 확정하면 안 됨. "교차검증 필요한 의사결정입니다. /cross-check 할까요?" 로 멈출 것.
- Claude 단독 판단으로 수치를 확정하지 않음 — 최소 2개 이종 모델 비교 필수

**품질검증 워크플로우 (강제):**
- 산출물 3개 이상 완성 시 자동으로 검증 제안: "산출물 검증이 필요합니다. 지금 할까요?"
- 검증 대상: 산출물 간 수치 정합성, 상호 모순, 누락, 과장
- 트랙 분리 작업 시: 각 트랙 완료 후 교차 검증 필수
- 배치 작업은 별도: 배치 완료 후 전체 산출물 교차검증 1회

**콘텐츠/생성 워크플로우:**
- 긴 문서(사업계획서, 발표대본 등): Claude 단독 작성 가능하되, 최종본은 교차검증 필수
- 짧은 산출물(설계문서, 코드): Claude 단독 OK
