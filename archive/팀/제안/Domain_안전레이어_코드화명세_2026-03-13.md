# v2 안전 레이어 코드화 명세서
> 작성: 최서영 (Domain Expert + Ethics/Safety Officer)
> 날짜: 2026-03-13
> 용도: 한재원(CTO) 구현용 — 안전 시스템 설계 70% → 구현 100% 달성 목표

---

## 1. 현황 진단

### 현재 구현 (v1)
| 항목 | 상태 | 위치 | 문제점 |
|------|------|------|--------|
| 위기 키워드 목록 | 10개 하드코딩 | `src/lib/types.ts` (CRISIS_KEYWORDS) | 리서치 대비 30% 미만 커버리지 |
| 위기 감지 로직 | 단순 `includes()` | `src/app/api/route-concern/route.ts` | 입력 시점 1회만 체크, 대화 중 미감지 |
| 위기 UI | CrisisAlert 모달 | `src/components/CrisisAlert.tsx` | 1393만 표시, Tier 구분 없음 |
| 대화 중 위기 감지 | 미구현 | — | 세션 진행 중 위기 발언 완전 무방비 |
| 면책 동의 | 푸터 1줄 | `src/app/page.tsx` footer | 법적 효력 없음, 동의 기록 없음 |

### 핵심 갭
1. **대화 중 위기 감지 = 0%** — `route-concern` API에서만 체크. `chat` API는 무방비.
2. **Tier 구분 없음** — 모든 위기를 동일하게 처리. "죽고 싶다"와 "약을 모으고 있다"가 같은 대응.
3. **면책 동의 미수집** — 서비스 이용 전 명시적 동의 절차 없음.

---

## 2. Tier 분류 체계

### 원칙
- **오탐 > 미탐.** 오탐은 UX 불편, 미탐은 생명 위험. 항상 보수적으로.
- 최악의 사용자 상태 기준: 밤에 혼자, 우울한 상태에서 이 입력을 한다면?

### Tier A — 즉시 중단 (Immediate Halt)
**정의**: 구체적 자살/자해 의도, 방법, 시점이 포함된 표현. 대화를 계속하면 안 되는 수준.

**트리거 조건**: 아래 키워드 중 1개 이상 매칭 시 즉시 발동.

```typescript
// src/lib/safety/crisis-keywords.ts

export const TIER_A_KEYWORDS: string[] = [
  // 직접 표현 — 자살
  "자살", "자살하고 싶", "자살 방법", "자살 시도", "자살 기도",
  "죽고 싶다", "죽고 싶어", "죽고싶다", "죽고싶어",
  "죽어버리고 싶", "죽어버리기", "죽어버렸으면",
  "나 죽으면", "다 죽어",
  // 직접 표현 — 자해
  "자해", "자해해", "자해 중", "자해 했", "자해 흔적",
  // 소멸 욕구 (직접적)
  "사라지고 싶다", "사라지고 싶어", "사라져버리고 싶",
  "없어지고 싶다", "없어지고 싶어",
  "숨고 싶다",
  // 구체적 방법/계획 시사
  "유서", "유서를 썼", "다 정리하고", "정리하고 가",
  "마지막을 준비", "오늘 밤에",
  // 온라인 은어 — 구체적 위험
  "컷팅", "자해러", "피뿌", "피뿌리기", "피분수",
  "자살 커뮤니티", "자살 모임", "자살 약속", "자살 브로커",
  "overdose",
];
```

**대응**: 즉시 대화 중단 + 전문기관 연결 화면 (CrisisAlert Tier A).

### Tier B — 주의 (Caution & Guide)
**정의**: 절망/무가치감/무력감 표현. 위험 신호이나 즉시 중단보다는 대화 유지하면서 자연스럽게 전문기관 안내.

**트리거 조건**: 아래 키워드 중 1개 이상 매칭 시 발동.

```typescript
export const TIER_B_KEYWORDS: string[] = [
  // 절망/무가치감
  "살 가치 없", "살 이유 없", "살고 싶지 않", "살고싶지않",
  "살기가 너무 힘", "나는 쓸모 없", "나는 쓰레기",
  "이제 끝이다", "이제 끝났", "나는 끝난 사람",
  "더는 못 버틴", "더는 못버틴",
  "이제 끝내고 싶", "세상이 싫",
  // 회피/도피 표현
  "도망가고 싶", "모든 게 싫",
  // 은어/변형
  "극단적 선택", "극단 선택", "소멸", "사라지기", "끝내기",
  "그만두기",
  // 감각 추구형 자해 암시
  "감각을 느끼고 싶", "몸으로라도 느끼고 싶", "몸으로 아픈 게 편",
];
```

**대응**: 대화 유지 + 안내 배너 삽입 + 프롬프트에 안전 가드 주입.

### 맥락 감지 필요 (Context-Required) — v2.1 이후
아래 키워드는 단독으로 반응하면 오탐이 높다. v2에서는 Tier에 포함하지 않되, 향후 LLM 기반 맥락 분석으로 처리.

```typescript
// 참고용. v2에서는 구현하지 않음.
export const CONTEXT_REQUIRED_KEYWORDS: string[] = [
  "칼", "긋기", "수면제", "과다 복용", "약을 모으",
  "죽으면 어떻게 되지",
];
```

---

## 3. 오케스트레이터 레벨 위기 감지 로직

### 아키텍처 변경

현재: `route-concern API → 키워드 체크 → crisis 응답`
변경: 모든 사용자 입력이 거치는 **공통 안전 레이어** 신설.

```
[사용자 입력]
     │
     ▼
┌─────────────────────┐
│  Safety Interceptor  │  ← 모든 입력의 첫 번째 관문
│  (crisisDetector)    │
└─────────┬───────────┘
          │
    ┌─────┴─────┐
    │           │
  Tier A     Tier B        Clean
    │           │             │
    ▼           ▼             ▼
  즉시 중단   안내 삽입     정상 진행
  + 모달      + 가드        (router/chat)
```

### 구현 명세

#### 3-1. `src/lib/safety/crisis-detector.ts` (신규)

```typescript
export type CrisisTier = "A" | "B" | null;

export interface CrisisDetectionResult {
  tier: CrisisTier;
  matchedKeyword: string | null;  // 디버깅용, 사용자에게 노출 금지
  response: CrisisResponse | null;
}

export interface CrisisResponse {
  // Tier A
  shouldHalt: boolean;           // true면 대화 즉시 중단
  // Tier B
  shouldInjectGuard: boolean;    // true면 프롬프트에 안전 가드 주입
  // 공통
  userMessage: string;           // 사용자에게 보여줄 메시지
  hotlines: Hotline[];           // 전문기관 연결 정보
  bannerType: "modal" | "inline" | null;
}

export interface Hotline {
  name: string;
  number: string;
  description: string;
}

export const HOTLINES: Hotline[] = [
  {
    name: "자살예방상담전화",
    number: "1393",
    description: "24시간 운영, 자살 위기 전문 상담",
  },
  {
    name: "정신건강위기상담전화",
    number: "1577-0199",
    description: "24시간 운영, 정신건강 위기 상담",
  },
  {
    name: "생명의전화",
    number: "1588-9191",
    description: "365일 24시간 상담",
  },
];

/**
 * 모든 사용자 입력에 대해 위기 키워드 체크를 수행한다.
 * 이 함수는 LLM 호출 전에 반드시 실행되어야 한다.
 *
 * 설계 원칙:
 * - 오탐 > 미탐. 의심 시 상위 Tier로 분류.
 * - matchedKeyword는 로깅용. 사용자에게 "이 단어 때문에" 노출 금지.
 * - 정규화: 공백 제거 + 유사 표현 매칭을 위해 normalized 검사 병행.
 */
export function detectCrisis(input: string): CrisisDetectionResult {
  const normalized = input.replace(/\s+/g, "");

  // Tier A 먼저 체크 (상위 우선)
  for (const keyword of TIER_A_KEYWORDS) {
    const normalizedKeyword = keyword.replace(/\s+/g, "");
    if (normalized.includes(normalizedKeyword) || input.includes(keyword)) {
      return {
        tier: "A",
        matchedKeyword: keyword,
        response: {
          shouldHalt: true,
          shouldInjectGuard: false,
          userMessage:
            "지금 많이 힘드시죠. 당신의 이야기를 들어줄 전문가가 있습니다. 지금 바로 연락해주세요.",
          hotlines: HOTLINES,
          bannerType: "modal",
        },
      };
    }
  }

  // Tier B 체크
  for (const keyword of TIER_B_KEYWORDS) {
    const normalizedKeyword = keyword.replace(/\s+/g, "");
    if (normalized.includes(normalizedKeyword) || input.includes(keyword)) {
      return {
        tier: "B",
        matchedKeyword: keyword,
        response: {
          shouldHalt: false,
          shouldInjectGuard: true,
          userMessage:
            "혼자 감당하기 어려운 마음이 있다면, 전문 상담사와 이야기하는 것도 방법이에요.",
          hotlines: HOTLINES,
          bannerType: "inline",
        },
      };
    }
  }

  return { tier: null, matchedKeyword: null, response: null };
}
```

#### 3-2. `src/app/api/route-concern/route.ts` (수정)

```typescript
import { detectCrisis } from "@/lib/safety/crisis-detector";

export async function POST(req: NextRequest) {
  const { input } = await req.json();
  if (!input || typeof input !== "string") {
    return NextResponse.json({ error: "입력이 필요합니다" }, { status: 400 });
  }

  // 안전 레이어 — 기존 CRISIS_KEYWORDS 대체
  const crisisResult = detectCrisis(input);
  if (crisisResult.tier === "A") {
    return NextResponse.json({
      crisis: true,
      tier: "A",
      message: crisisResult.response!.userMessage,
      hotlines: crisisResult.response!.hotlines,
    });
  }

  try {
    const result = await routeConcern(input);

    // Tier B: 라우팅 결과에 안전 가드 플래그 추가
    if (crisisResult.tier === "B") {
      return NextResponse.json({
        ...result,
        safetyGuard: {
          tier: "B",
          message: crisisResult.response!.userMessage,
          hotlines: crisisResult.response!.hotlines,
        },
      });
    }

    return NextResponse.json(result);
  } catch (error) {
    console.error("Routing error:", error);
    return NextResponse.json(
      { error: "AI 서비스에 일시적인 문제가 있습니다." },
      { status: 500 }
    );
  }
}
```

#### 3-3. `src/app/api/chat/route.ts` (수정 — 핵심 갭 해소)

**현재 `chat` API에는 위기 감지가 없다.** 이것이 가장 큰 갭이다.

```typescript
import { detectCrisis } from "@/lib/safety/crisis-detector";

export async function POST(req: NextRequest) {
  const { messages, stage, modelType } = await req.json();

  // [신규] 마지막 사용자 메시지에 대해 위기 감지
  const lastUserMsg = [...messages].reverse().find((m: Message) => m.role === "user");
  if (lastUserMsg) {
    const crisisResult = detectCrisis(lastUserMsg.content);

    if (crisisResult.tier === "A") {
      // Tier A: 스트리밍 대신 즉시 위기 응답 반환
      const encoder = new TextEncoder();
      const crisisStream = new ReadableStream({
        start(controller) {
          controller.enqueue(
            encoder.encode(`data: ${JSON.stringify({
              text: crisisResult.response!.userMessage,
              crisis: true,
              tier: "A",
              hotlines: crisisResult.response!.hotlines,
            })}\n\n`)
          );
          controller.enqueue(encoder.encode("data: [DONE]\n\n"));
          controller.close();
        },
      });
      return new Response(crisisStream, {
        headers: { "Content-Type": "text/event-stream" },
      });
    }

    if (crisisResult.tier === "B") {
      // Tier B: 시스템 프롬프트에 안전 가드 주입
      const safetyGuardPrompt = `
[안전 가드 — 활성]
사용자가 심리적으로 힘든 상태일 수 있습니다.
다음을 반드시 지켜주세요:
1. 절대 고통을 축소하거나 "다 괜찮아질 거예요" 같은 빈말 금지
2. 사용자의 감정을 먼저 인정 ("많이 힘드시죠", "그 마음 충분히 이해해요")
3. 응답 마지막에 자연스럽게 한 줄 삽입: "혼자 감당하기 어려우시면, 1393(자살예방상담전화)에서 24시간 이야기를 들어줍니다."
4. 질문은 가볍게, 깊이 파고들지 않기
5. "안 한다" 옵션을 항상 제시
`;
      // systemPrompt에 safetyGuardPrompt를 prepend
      const systemPrompt = safetyGuardPrompt + "\n\n" + buildSystemPrompt(stage, modelType);
      // 이하 기존 LLM 호출 로직에 systemPrompt 교체 적용
    }
  }

  // ... 기존 로직 계속
}
```

---

## 4. 에스컬레이션 경로

### Tier A 에스컬레이션 플로우

```
사용자 입력 → Tier A 감지
     │
     ▼
[1] 대화 즉시 중단 (LLM 호출 안 함)
     │
     ▼
[2] CrisisAlert 모달 표시 (풀스크린, 닫기 불가 5초)
     │
     ├─ [전화하기 1393] → tel:1393 바로 연결
     ├─ [전화하기 1577-0199] → tel:1577-0199
     ├─ [카카오톡 상담] → https://www.0199.or.kr (정신건강위기상담 카톡)
     └─ [5초 후] "지금은 괜찮아요" 버튼 활성화
              │
              ▼
         인라인 배너로 전환 (세션 상단 고정)
         "언제든 전문 상담이 필요하시면 1393으로 연락하세요"
```

### Tier B 에스컬레이션 플로우

```
사용자 입력 → Tier B 감지
     │
     ▼
[1] 대화 계속 (LLM 호출 진행, 안전 가드 프롬프트 주입)
     │
     ▼
[2] 인라인 안내 배너 삽입 (채팅 영역 상단)
     "혼자 감당하기 어려운 마음이 있다면, 전문 상담사와 이야기하는 것도 방법이에요."
     [1393 전화하기] [자세히 보기]
     │
     ▼
[3] AI 응답에 자연스러운 전문기관 안내 포함 (프롬프트 가드에 의해)
     │
     ▼
[4] 세션 종료 시 — 리포트에 전문기관 안내 섹션 자동 추가
```

### 로깅 (v2.1 — Supabase 연동 후)

```typescript
// 향후 구현. v2에서는 로컬 로깅만.
interface CrisisLog {
  sessionId: string;
  timestamp: string;
  tier: "A" | "B";
  matchedKeyword: string;    // 내부 분석용, 사용자에게 노출 금지
  userAction: "called" | "dismissed" | "continued";
}
```

---

## 5. 면책 동의 UX 명세

### 진입 시점
사용자가 **첫 번째 고민을 입력하기 전**, 1회 동의.

### 동의 내용 (법적 검토 필요 — L1 승인 사항)

```
[풀림 서비스 이용 안내]

풀림은 AI 기반 의사결정 도우미로, 전문 심리상담이나 치료를
대체하지 않습니다.

1. 이 서비스는 웰니스/코칭 목적이며, 의료적 진단이나
   치료를 제공하지 않습니다.
2. 의사결정의 최종 책임은 이용자 본인에게 있습니다.
3. 긴급한 심리적 위기 시, 자살예방상담전화(1393) 또는
   정신건강위기상담전화(1577-0199)로 연락하세요.
4. 대화 내용은 서비스 개선 목적으로 익명화하여 활용될 수
   있습니다.

[동의하고 시작하기]
```

### 구현 명세

#### 5-1. 동의 상태 관리

```typescript
// src/lib/store/session-store.ts에 추가

interface SessionState {
  // ... 기존 필드
  disclaimerAgreed: boolean;
  disclaimerAgreedAt: string | null;
}

// 액션
agreeToDisclaimer: () => void;
```

`disclaimerAgreed`가 `false`이면 고민 입력 비활성화. localStorage에 persist.

#### 5-2. 동의 UI 컴포넌트

```typescript
// src/components/DisclaimerModal.tsx (신규)

interface Props {
  onAgree: () => void;
}

// 디자인 가이드:
// - 풀스크린 모달 (하단에서 슬라이드업, 모바일 친화)
// - 텍스트: 16px, 행간 1.6, 읽기 쉽게
// - [동의하고 시작하기] 버튼: bg-violet-600, 하단 고정
// - 스크롤 끝까지 내려야 버튼 활성화 (내용 읽기 유도)
// - X 버튼 없음. 동의해야만 진행 가능.
```

#### 5-3. 홈페이지 연동

```typescript
// src/app/page.tsx 수정

// handleSubmit 시작 부분에:
if (!disclaimerAgreed) {
  setShowDisclaimer(true);
  return;
}
```

---

## 6. UI 컴포넌트 변경 명세

### 6-1. CrisisAlert.tsx 리팩토링

현재: 단일 모달, props로 message/hotline 받음.
변경: Tier A/B 분기 + 다중 핫라인 + 5초 딜레이.

```typescript
// src/components/CrisisAlert.tsx

interface Props {
  tier: "A" | "B";
  message: string;
  hotlines: Hotline[];
  onDismiss: () => void;  // Tier A: 5초 후 활성화 / Tier B: 즉시 가능
}

// Tier A:
//   - 배경: bg-black/70 (더 어둡게)
//   - 닫기 버튼: 5초 후 활성화 (카운트다운 표시)
//   - 핫라인 3개 모두 표시 (전화 + 카톡)
//   - "지금은 괜찮아요" 텍스트 (5초 후)

// Tier B:
//   - 인라인 배너 (채팅 영역 상단, sticky)
//   - 배경: bg-amber-50 border-amber-200
//   - 핫라인 1개 (1393) + [더보기] 링크
//   - X로 닫기 가능 (닫아도 세션 내 1회 더 노출)
```

### 6-2. SafetyBanner.tsx (신규 — Tier B 인라인 배너)

```typescript
// src/components/SafetyBanner.tsx

interface Props {
  message: string;
  hotlines: Hotline[];
  onDismiss: () => void;
  dismissCount: number;  // 0이면 첫 노출, 1이면 재노출 후 완전 숨김
}

// 위치: 세션 페이지 채팅 영역 상단, sticky
// 색상: bg-amber-50, text-amber-800, border-amber-200
// 레이아웃: 아이콘(하트) + 메시지 + [1393 전화] + [X]
```

---

## 7. 파일 구조 변경 요약

```
src/lib/
  safety/                          ← 신규 디렉토리
    crisis-keywords.ts             ← Tier A/B 키워드 목록 (하드코딩)
    crisis-detector.ts             ← detectCrisis() 함수
    types.ts                       ← CrisisTier, CrisisResponse 등 타입

src/lib/types.ts
  → CRISIS_KEYWORDS 제거 (safety/crisis-keywords.ts로 이동)

src/lib/store/session-store.ts
  → disclaimerAgreed, disclaimerAgreedAt 필드 추가
  → agreeToDisclaimer 액션 추가

src/app/api/route-concern/route.ts
  → CRISIS_KEYWORDS import 제거
  → detectCrisis import + Tier A/B 분기

src/app/api/chat/route.ts
  → detectCrisis import + 매 메시지 위기 감지
  → Tier B 시 safetyGuardPrompt 주입

src/components/
  CrisisAlert.tsx                  ← Tier A/B 분기 리팩토링
  SafetyBanner.tsx                 ← 신규 (Tier B 인라인)
  DisclaimerModal.tsx              ← 신규 (면책 동의)

src/app/page.tsx
  → DisclaimerModal 연동
  → CrisisAlert에 tier/hotlines props 변경

src/app/session/[id]/page.tsx
  → chat API 응답에서 crisis 플래그 처리
  → SafetyBanner 렌더링
  → Tier A 감지 시 CrisisAlert 모달 표시
```

---

## 8. 테스트 케이스 (한재원 구현 시 검증용)

| # | 입력 | 예상 Tier | 예상 동작 |
|---|------|----------|----------|
| 1 | "자살하고 싶다" | A | 즉시 중단 + 모달 |
| 2 | "죽고싶어" | A | 즉시 중단 + 모달 |
| 3 | "컷팅했어" | A | 즉시 중단 + 모달 |
| 4 | "살 가치가 없다" | B | 대화 계속 + 인라인 배너 + 가드 프롬프트 |
| 5 | "이제 끝내고 싶다" | B | 대화 계속 + 인라인 배너 |
| 6 | "이직할까 말까" | null | 정상 진행 |
| 7 | "칼을 사야 할까" (요리 맥락) | null | 정상 진행 (맥락 키워드는 v2 미구현) |
| 8 | "죽 고 싶 다" (공백 삽입 우회) | A | 정규화로 감지 → 즉시 중단 |
| 9 | 세션 3번째 메시지에서 "자살" | A | chat API에서 감지 → 즉시 중단 |
| 10 | 면책 미동의 상태에서 입력 시도 | — | DisclaimerModal 표시, 입력 차단 |

---

## 9. 승인 필요 사항 (L1)

| 항목 | 이유 | 필요 승인자 |
|------|------|-----------|
| 면책 동의 문구 확정 | 법적 효력 관련 — 전문 법률 자문 권고 | 대표 |
| Tier A 키워드 목록 확정 | 위기 감지 기준 변경 = L0 사항 (서영 + 대표) | 대표 + 서영 |
| Tier B → A 승격 기준 | 반복 Tier B 감지 시 A로 에스컬레이션 여부 | 대표 + 서영 |
| 로깅 범위 | 개인정보보호법 관련 — 어디까지 저장할 것인가 | 대표 |

---

## 10. 다음 행동

1. **한재원(CTO)**: 이 명세 기반으로 `src/lib/safety/` 디렉토리 + `crisis-detector.ts` 구현 착수. `chat` API에 위기 감지 삽입이 최우선 — 현재 완전 무방비 상태.

2. **대표**: 면책 동의 문구 검토 + Tier A 키워드 목록 최종 승인. 법률 자문이 필요한지 판단. (Perplexity에서 "AI 웰니스 앱 면책 조항 한국 법률" 검색 권고)

3. **서윤하(AI Lead)**: Tier B 안전 가드 프롬프트(`safetyGuardPrompt`) 톤 검토. "감정 인정 → 전문기관 안내" 흐름이 풀림 톤과 일관되는지 확인.
