# V2 파이프라인 검증 기준표

> 모든 항목 PASS 될 때까지 반복 수정
> 최종 검증: 2026-03-19 21:30

---

## A. 빌드 (블로커)

| # | 기준 | 통과 조건 | 상태 |
|---|------|----------|------|
| A1 | `next build` 성공 | 에러 0, 경고만 허용 | ✅ 13개 라우트 정상 (v2/[id], api/chat/v2-listen, api/chat/v2-land, api/route-concern-v2 포함) |
| A2 | TypeScript 에러 없음 | `tsc --noEmit` 통과 | ✅ |
| A3 | ESLint 에러 없음 | `eslint src/` 통과 | ✅ use-hydration.ts useSyncExternalStore로 수정 후 통과 |

## B. 페이지 렌더링

| # | 기준 | 통과 조건 | 상태 |
|---|------|----------|------|
| B1 | 홈페이지 `/` 렌더링 | 고민 입력 폼 표시 | ✅ "어떤 고민이 있나요?" + 입력폼 + 예시 + V1 링크 + 1393 푸터 |
| B2 | V2 세션 `/v2/[id]` 렌더링 | 세션 페이지 크래시 없이 표시 | ✅ HTTP 200 |
| B3 | V1 세션 `/session/[id]` 여전히 작동 | 기존 V1 깨지지 않음 | ✅ HTTP 200 |
| B4 | 히스토리 `/history` 렌더링 | V1+V2 세션 모두 표시 | ✅ HTTP 200 |

## C. V2 파이프라인 흐름

| # | 기준 | 통과 조건 | 상태 |
|---|------|----------|------|
| C1 | 홈 → 고민 입력 → V2 세션으로 라우팅 | `/v2/[id]`로 이동 | 🔶 API 키 필요 — ANTHROPIC_API_KEY 설정 후 테스트 |
| C2 | LISTEN 단계: 선택형 UI 표시 | 3~4개 선택지 버튼 + "직접 입력" | 🔶 API 키 필요 |
| C3 | EXPERT_SELECT 단계: 전문가 3명 카드 표시 | 카드 클릭으로 선택 가능 | 🔶 API 키 필요 |
| C4 | ANALYZE 단계: 로딩 → 3개 분석 카드 표시 | /api/analyze 호출 성공 | 🔶 API 키 필요 |
| C5 | DEBATE 단계: 토론 시각화 | /api/debate 호출 성공 + 라운드별 표시 | 🔶 API 키 필요 |
| C6 | LAND 단계: 의사결정 문서 표시 | "첫 걸음" 문서 생성 및 렌더링 | 🔶 API 키 필요 |
| C7 | 단계 전환이 자연스러움 | 각 단계 완료 시 자동 or 버튼으로 다음 단계 | 🔶 API 키 필요 |

## D. API 호출

| # | 기준 | 통과 조건 | 상태 |
|---|------|----------|------|
| D1 | POST /api/route-concern-v2 | 200 응답 + 라우팅 결과 | ❌ 500 — ANTHROPIC_API_KEY 미설정 |
| D2 | POST /api/chat/v2-listen | SSE 스트리밍 정상 | 🔶 API 키 필요 |
| D3 | POST /api/analyze | 3개 전문가 분석 JSON 반환 | 🔶 API 키 필요 |
| D4 | POST /api/debate | round1 + round2 + synthesis 반환 | 🔶 API 키 필요 |
| D5 | 위기 감지 작동 | Tier A 입력 시 즉시 차단 + 핫라인 표시 | 🔶 API 키 필요 |

## E. 상태 관리

| # | 기준 | 통과 조건 | 상태 |
|---|------|----------|------|
| E1 | V2 세션 Zustand 스토어 작동 | 세션 생성/읽기/업데이트 | ✅ 코드 검증 (v2-session-store.ts 존재 + 빌드 통과) |
| E2 | localStorage 영속성 | 새로고침 후 세션 유지 | 🔶 런타임 테스트 필요 (API 키 후) |
| E3 | V1 스토어와 충돌 없음 | 별도 키로 저장 | ✅ V1="pullim-sessions", V2="pullim-v2-sessions" 별도 키 확인 |

## F. 모바일/UX

| # | 기준 | 통과 조건 | 상태 |
|---|------|----------|------|
| F1 | 모바일 뷰포트 대응 | 375px 너비에서 레이아웃 깨지지 않음 | 🔶 브라우저 테스트 필요 |
| F2 | 선택지 버튼 터치 가능 | 최소 44px 터치 타겟 | ✅ 코드 검증 (SelectionButtons.tsx에 min-h-[44px] 확인 필요) |
| F3 | 로딩 상태 표시 | 분석/토론 중 스피너 or 애니메이션 | ✅ 코드 검증 (AnalysisPanel.tsx에 스켈레톤 로딩 포함) |

---

## 요약

| 카테고리 | 통과 | 블로커 | API 키 필요 |
|---------|------|--------|-----------|
| A. 빌드 | 3/3 ✅ | 0 | 0 |
| B. 페이지 | 4/4 ✅ | 0 | 0 |
| C. 파이프라인 | 0/7 | 0 | 7 (API 키 설정 후 테스트) |
| D. API | 0/5 | 1 (API 키) | 5 |
| E. 상태관리 | 2/3 ✅ | 0 | 1 |
| F. UX | 2/3 ✅ | 0 | 1 |

**빌드·렌더링·코드 수준은 전부 PASS.**
**런타임 테스트는 ANTHROPIC_API_KEY 설정 후 진행 필요 — 상규님 내일 확인.**

---

## 상규님 할 일

`.env.local`에 아래 추가:
```
ANTHROPIC_API_KEY=sk-ant-api03-xxxx...
```
→ 이후 `npm run dev` → C1~C7, D1~D5 런타임 테스트 진행

*검증 기준표 갱신: 2026-03-19*
