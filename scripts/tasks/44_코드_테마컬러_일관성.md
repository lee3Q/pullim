# 테마별 컬러 일관성 점검 + 수정

## 프로젝트
풀림 (~/study/main/pullim/pullim/) — Next.js 15 + React 19 + TypeScript + Tailwind CSS 4

## 확정 컬러
- garden (달빛정원): #a78bfa (보라)
- adventure (모험가): #ff9f1c (주황)
- strategy (전략실): #60a5fa (파랑)
- 공통 배경: #12101a (다크)
- 공통 accent: #7c3aed (보라 진함)

## 작업
1. 다음 파일들에서 테마 컬러 사용 현황 확인:
   - page.tsx (THEME_COLORS 맵)
   - StoryDiscovery.tsx (primaryColor prop)
   - LadderSession.tsx
   - Level1~5 컴포넌트
   - globals.css (CSS 변수)

2. 불일치 발견 시 수정:
   - 하드코딩된 컬러 → 위 확정 컬러로 통일
   - CSS 변수로 관리되는 건 변수 값만 수정

## 주의
- 색상값만 수정, 레이아웃 변경 없음
- `npm run build` 통과 확인
