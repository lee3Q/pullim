# 아웃트로 화면 개선 (유형 결과로 연결)

## 프로젝트
풀림 (~/study/main/pullim/pullim/) — Next.js 15 + React 19 + TypeScript + Tailwind CSS 4

## 선행 작업
36번에서 PersonalityResult.tsx, personality-type.ts가 생성되어 있어야 함

## 현재 상태
StoryDiscovery.tsx의 outro 단계: 일러스트 + "파악 완료" 메시지 → 2.5초 후 자동으로 onComplete 호출

## 작업
아웃트로에서 유형 결과로 자연스럽게 전환되도록 개선:

1. outro 단계 텍스트를 "당신의 이야기를 읽고 있어요..." 같은 분석 중 느낌으로 변경
2. 2.5초 대기 시간에 로딩 느낌 추가 (점 애니메이션: "분석 중...")
3. 이후 page.tsx에서 result 단계로 자연스럽게 전환

## 주의
- StoryDiscovery.tsx의 outro phase 텍스트/스타일만 수정
- onComplete 타이밍이나 로직은 변경하지 말 것
- 36번 선행 없이도 빌드는 통과해야 함
- `npm run build` 통과 확인
