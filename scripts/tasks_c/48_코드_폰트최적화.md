# 폰트 서브셋 최적화

## 프로젝트
풀림 (~/study/main/pullim/pullim/) — Next.js 15

## 작업
1. layout.tsx에서 폰트 로드 방식 확인 (Google Fonts CDN? next/font?)
2. Gowun Batang + Noto Serif KR이 사용됨
3. Next.js의 `next/font/google` 사용으로 전환 (이미 되어 있으면 확인만):
   - 자동 서브셋팅
   - self-hosting (CDN 의존 제거)
   - font-display: swap
4. 이미 next/font으로 되어 있으면 → subsets: ['latin', 'korean'] 확인

## 주의
- 폰트 변경은 디자인에 영향 → 같은 폰트 유지, 로드 방식만 최적화
- `npm run build` 통과 확인
