# UI 피드백 수집 준비 — 작업 하네스

> 생성: 2026-03-27
> 목표: 파악 심리테스트를 외부에 공유할 수 있는 상태로 만들기 (품질검증 + 플로우 테스트 + 공유 최적화)
> 마감: 없음 (다음 세션)
> 방식: 단일 세션

---

## 공유 Context
| 파일 | 역할 |
|------|------|
| `.state/projects/풀림_핵심UX_설계.md` | 프로젝트 상태 (4단계 실행) |
| `pullim/src/components/discovery/PersonalityResult.tsx` | 유형 결과 카드 (테마별 이름+태도+이미지+스탯바) |
| `pullim/src/lib/personalization/personality-type.ts` | 8유형 × 3테마 데이터 |
| `pullim/src/lib/personalization/story-scenes.ts` | 파악 30장면 데이터 |
| `pullim/src/components/discovery/StoryDiscovery.tsx` | 스토리형 파악 컴포넌트 |
| `pullim/src/components/discovery/FeedbackForm.tsx` | 익명 피드백 폼 |
| `pullim/src/app/page.tsx` | 홈 → 파악 → 결과 흐름 제어 |

## 이전 세션 완료 사항 (2026-03-27)
| 항목 | 상태 |
|------|------|
| 밤 배치 28개 머지+배포 | ✅ |
| 접근성 대비 수정 (태스크 45) | ✅ |
| 테마별 유형 이름+이모지+태도 (personality-type.ts) | ✅ |
| 결과 카드 비주얼 업그레이드 (그라데이션+글로우+스탯바) | ✅ |
| 캐릭터 이미지 로딩 (폴백: 이모지) | ✅ 코드 준비, 이미지는 대표가 p도구로 생성 중 |
| p도구 prompts.txt + NAMES 업데이트 | ✅ |

---

## 1층: 품질검증 (의존: 없음) ✅

#### 1. 배치 결과 주요 항목 코드 리뷰 ✅
- **읽기**: 배치 커밋 diff (`git diff 559099e..0a795ba -- pullim/src/`)
- **할 일**: 유형결과화면(36), 익명피드백폼(38), 데모모드응답개선(53), 홈화면데모유도(34) 중심으로 코드 품질 확인. 빌드는 이미 통과했으므로 로직/UX 흐름만 체크.
- **산출물**: 수정 필요 시 즉시 수정 (별도 파일 불필요)
- **핵심**: 외부 사용자가 보게 될 화면이므로 깨지는 UI나 에러 없는지 확인

#### 2. 캐릭터 이미지 확인 + 연결 ✅
- **읽기**: `pullim/public/images/personality/` 폴더
- **할 일**: 대표가 p도구로 생성한 이미지가 들어와 있는지 확인. 있으면 빌드 테스트. 없으면 이모지 폴백으로 진행.
- **산출물**: 빌드 통과 확인
- **핵심**: 이미지 파일명이 `{theme}_{type-id}.png` 형식인지 확인 (PersonalityResult.tsx가 이 경로로 로드함)

---

## 2층: 플로우 테스트 (의존: 1층) ✅

#### 3. 홈→파악→결과→피드백 전체 플로우 E2E 테스트 ✅
- **읽기**: `pullim/src/app/page.tsx` (홈), `StoryDiscovery.tsx`, `PersonalityResult.tsx`, `FeedbackForm.tsx`
- **할 일**: Playwright 또는 수동으로 3개 테마 각각 테스트. 체크리스트:
  - [ ] 홈에서 테마 선택 가능
  - [ ] 10장면 끝까지 진행 가능 (중간 완료도 테스트)
  - [ ] 아웃트로 → 결과 카드 정상 표시
  - [ ] 테마별 유형명이 다르게 나오는지 확인
  - [ ] "풀림이 출시되면, 당신에게는" 태도 메시지 표시
  - [ ] 4축 스탯바 정상 렌더링
  - [ ] 공유 버튼 동작 (clipboard 복사)
  - [ ] 피드백 폼 전송 → Supabase 저장 확인
  - [ ] "대화 시작하기" → 사다리 세션 진입
- **산출물**: 버그 발견 시 즉시 수정
- **핵심**: 외부 사용자가 처음 접속해서 끝까지 갈 수 있는지

---

## 3층: 공유 최적화 (의존: 2층) ✅

#### 4. OG 메타 동적화 — 유형별 공유 미리보기 ✅
- **읽기**: `pullim/src/app/layout.tsx`, `pullim/src/app/page.tsx`
- **할 일**: 결과 페이지 URL에 유형 정보를 쿼리 파라미터로 포함시키고, OG 메타태그를 동적으로 생성. 카카오톡/인스타 공유 시 "나는 지도를 먼저 펴는 모험가래!" 같은 제목+설명이 뜨도록.
- **산출물**: `pullim/src/app/result/page.tsx` (신규, 동적 OG) 또는 기존 page.tsx 수정
- **핵심**: 공유 링크 클릭 시 바로 결과 확인 가능해야 함 (파악 다시 안 해도)
- **⚠️**: URL 구조 변경이므로 기존 라우팅에 영향 없는지 확인

#### 5. 결과 공유 텍스트 개선 ✅
- **읽기**: `PersonalityResult.tsx` 내 handleShare/handleCopy
- **할 일**: 공유 텍스트를 더 매력적으로. "풀림이 알아낸 나의 유형" → 테마별 맞춤 문구. 예: "달빛정원에서 발견한 나 — 포근한 달빛을 가진 사람"
- **산출물**: PersonalityResult.tsx 수정
- **핵심**: 받는 사람도 해보고 싶게 만드는 문구

---

## 4층: 마무리 (의존: 3층) — 진행 중

#### 6. 프로젝트 파일 갱신 ✅
- **읽기**: `.state/projects/풀림_핵심UX_설계.md`, `.state/projects/아산두어스_신청.md`
- **할 일**:
  - 풀림_핵심UX_설계: UI 피드백 수집 단계 추가, 완료 항목 체크
  - 아산두어스_신청: 수집 로그에 "심리테스트형 파악 + 공유 기능으로 외부 피드백 루프 구축" 추가
- **산출물**: 프로젝트 파일 2개 갱신
- **핵심**: 다음 세션이 현재 상태를 정확히 파악할 수 있게

#### 7. 커밋 + 푸시 + Vercel 배포 확인
- **할 일**: 모든 변경사항 커밋, push, Vercel 빌드 성공 확인
- **산출물**: 배포된 pullim.vercel.app에서 최종 확인

---

## 실행 규칙
1. 리서치 워크플로우 해당 없음 (코딩/테스트 작업)
2. 교차 모델 검증 해당 없음 (UI 작업)
3. 기존 파일 덮어쓰기 금지 (decisions/, sessions/)
4. 각 작업 완료 시 이 하네스 상태 ✅ 갱신
5. OG 메타 작업(4번)은 URL 구조 변경이므로 신중하게
