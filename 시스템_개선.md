# 시스템 개선점 — 2026-04-04

> Claude Code 폴더 구조 전수 탐색 후 도출. 풀림 + 개인 시스템 전체 대상.

---

## 즉시 실행 가능

### 1. MEMORY.md MCP 섹션 드리프트
- **현상**: MEMORY.md에 "slack, notion, context7, sequential-thinking, playwright, google-workspace" 기록되어 있지만, 실제 `mcp.json`에는 google-workspace, ouroboros, telegram 3종만 존재
- **조치**: MEMORY.md를 현재 상태로 업데이트

### 2. PostToolUse Hook 비효율
- **현상**: `context-auto-save.sh`가 Edit/Write/Bash/Agent 매 호출마다 실행 (3s timeout). 대부분 80% 미만이라 no-op 종료
- **조치**: PostToolUse에서 제거하고 PreCompact + Stop hook에서만 실행. 또는 마커 파일에 타임스탬프 추가하여 5분 이내 재실행 스킵

### 3. ouroboros 업데이트
- **현상**: v0.25.2 → v0.27.0 사용 가능
- **조치**: `ooo update` 실행

### 4. 상태 파일 정리 스크립트
- **현상**: `.state/decisions/`, `.state/handoffs/`, `세션_백업/` 모두 append-only, pruning 없음
- **조치**: 30일 이상 → `_archive/` 자동 이동, `세션_백업/` 7일 retention 후 gzip, 주간 회의록 월별 merge

### 5. Telegram MCP 중복
- **현상**: mcp-telegram (API 직접) + 공부알림봇 + 도서근무알림봇 = 3개 프로세스
- **조치**: mcp-telegram이 범용이면 봇 2개를 통합. 아니면 역할을 명확히 분리하고 MEMORY.md에 기록

---

## 계획 필요

### 6. CLAUDE.md 규칙 중복 (7개 파일)
- **현상**: 안전 규칙(위기 감지, 삭제 금지, sycophancy 방지)이 pullim/CLAUDE.md, 노무사/CLAUDE.md, 일상/건강/CLAUDE.md 등에 각각 반복
- **위험**: 규칙 하나 수정 시 7곳 수정 필요 → 불일치 발생
- **개선안**: `study/main/CLAUDE.md`에 공통 규칙을 한 번만 정의. 도메인별 CLAUDE.md는 고유 규칙만 포함 (Claude Code는 상위 CLAUDE.md 자동 상속)

### 7. 메모리 파편화 — 프로젝트 간 기억 단절
- **현상**: 메모리가 5개 프로젝트 디렉토리에 분산 (study, study-main-pullim, study-main, health, root)
- **위험**: 풀림 작업 중 노무사 진행 상황 모름, 건강 세션에서 풀림 스트레스 맥락 놓침
- **개선안**: root 메모리(`-Users-sanggyulee/memory/`)에 cross-project index 도입. 각 프로젝트 메모리에서 교차 영향 사항만 참조 링크로 연결

### 8. 풀림 이면사고 모델 티어링
- **현상**: ICC 세션 매 턴마다 Opus로 이면사고 실행 (~186원/세션)
- **개선안**: 일반 턴 → Haiku, 레벨 전환 판단 → Sonnet, 세션 종합 분석 → Opus. 예상 절감: ~120원/세션

### 9. PMF 자동 대시보드
- **현상**: PMF 성공 기준 정의됨 (완료율 >40%, 7일 리턴 >15-20%, Sean Ellis >30%) 하지만 자동 수집 없음
- **개선안**: Supabase MCP + cron trigger로 주간 PMF 대시보드 자동 생성. night-meeting에 PMF 지표 섹션 추가

### 10. GLM-5 cross-check 단일장애점
- **현상**: `/cross-check`의 GLM-5 호출이 z.ai 게이트웨이 의존
- **위험**: z.ai 장애 시 cross-check 전체 실패
- **개선안**: fallback 모델 지정 (Gemini Flash via API key 직접). `try GLM-5 → catch → fallback Gemini` 로직

### 11. 창업/비창업 자동 모드 전환
- **현상**: `claude-mode.sh`로 수동 전환. 전환 잊으면 맥락 불일치
- **개선안**: 작업 디렉토리 기반 자동 라우팅 — `pullim/` 진입 시 창업 모드, `일상/` 진입 시 비창업 모드

---

## 메타 인사이트

> 시스템이 "실행"에는 매우 강하지만 "유지보수"에는 약함.
> append-only 정책, 분산된 규칙, 업데이트 안 된 메모리 — 모두 **엔트로피 축적** 문제.
> 1인 시스템에서는 "주간 시스템 정비" 루틴을 night-meeting 같은 자동화로 돌리는 게 가장 효과적.
