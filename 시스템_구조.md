# 풀림 시스템 구조 레퍼런스

> 최종 갱신: 2026-03-20
> 용도: "이게 뭐였지?" 할 때 보는 문서. 전체 그림을 한 곳에.

---

## 1. 전체 구조

```
pullim/                          ← 프로젝트 루트
├── CLAUDE.md                    ← Claude에게 주는 지시서 (워크플로우 규칙, 원칙)
│
├── .state/                      ← 상태 관리 (핵심)
│   ├── project-config.md        ← 불변 설정 (제품 정의, 안전규칙, AI 스택, 금지사항)
│   ├── projects/                ← 프로젝트별 상태 파일 (진행 중인 것)
│   │   ├── ICC_지원.md          ← 현재 active
│   │   ├── _done/              ← 완료된 프로젝트 보관
│   │   └── _backlog/           ← 아이디어/대기
│   ├── decisions/               ← 의사결정 기록 (추가만, 덮어쓰기 금지)
│   ├── handoffs/                ← 세션 핸드오프 문서 (추가만)
│   └── sessions/                ← 세션 기록
│
├── .claude/                     ← Claude Code 설정
│   ├── settings.json            ← 프로젝트 hook 설정
│   ├── hooks/                   ← hook 스크립트
│   │   ├── session-start.sh     ← 세션 시작 시 자동 컨텍스트 로드
│   │   ├── cross-check-guard.sh ← 신청서/제품 편집 시 교차검증 알림
│   │   └── research-guard.sh    ← 리서치 단독 진행 방지
│   └── commands/
│       └── harness.md           ← /harness 스킬 (작업 분해)
│
├── pullim/                      ← Next.js 웹앱 소스코드
│   └── src/
│
├── 제품/                        ← 제품 설계 문서
├── 리서치/                      ← 리서치 산출물
├── 신청서/                      ← 사업계획서, 발표대본 등 외부 제출물
├── 테스트/                      ← 풀림 테스트 세션 기록
├── 사전/                        ← 풀림 용어 사전
├── scripts/                     ← 배치 스크립트, 하네스, 자동화
└── archive/                     ← 이전 팀 자료 (사용 안 함)
```

---

## 2. 세션 라이프사이클

```
세션 시작                           세션 종료
   │                                   │
   ▼                                   ▼
[SessionStart hook]              [/handoff 실행]
자동으로 프로젝트 상태 +              핸드오프 문서 생성
핸드오프 요약 로드                    (.state/handoffs/)
   │                                   │
   ▼                                   ▼
Claude가 .state/ 읽고 브리핑      프로젝트 파일 갱신
   │                              (.state/projects/)
   ▼                                   │
"어떤 작업부터 할까?"              "다음 세션에서 /pullim으로 이어가세요"
```

- `/pullim` — 수동으로 세션 시작 (더 상세한 브리핑)
- SessionStart hook — 자동으로 최소 컨텍스트 로드 (/pullim 안 쳐도)
- `/handoff` — 세션 종료 시 반드시 실행. compact 전에도

---

## 3. 워크플로우 강제 규칙

### 프로젝트 단계 강제
각 프로젝트 파일(.state/projects/)에 단계가 명시됨:
```
1. 리서치 → 2. 논의 → 3. 검증 → 4. 실행 → 5. 최종 검증
```
- 순서 건너뛰기 금지
- 변경하고 싶으면 제안 → 대표 승인 → 파일 갱신 후 실행

### 리서치 워크플로우
- **Claude 단독 리서치 금지**
- 최소: "Perplexity/Gemini 프롬프트 드릴까요?" 확인 후 병렬 진행
- hook 강제: WebSearch/WebFetch 시도 시 자동 알림 (research-guard.sh)

### 의사결정 검증 (/cross-check)
아래 4가지 상황에서 반드시 교차검증:
1. 비용/수치 결정 (가격, BEP, 마진, API 비용)
2. 기술 스택 선택 (모델, 도구, 아키텍처)
3. 전략 방향 전환 (피벗, 포지셔닝, 타겟)
4. 외부 제출물 확정 (사업계획서, 발표대본)

- hook 강제: 신청서/제품 파일 편집 시 자동 알림 (cross-check-guard.sh)
- 실행: `/cross-check` → Claude 판단 + GLM-5 판단 비교 → 불일치점 도출

### 품질검증
- 산출물 3개 이상 완성 시 자동으로 검증 제안 (배치 작업은 완료 후 전체 1회)
- 검증 대상: 수치 정합성, 상호 모순, 누락, 과장

### 콘텐츠/생성
- 긴 문서 (사업계획서, 발표대본): 작성은 Claude OK, 최종본은 교차검증 필수
- 짧은 산출물 (설계문서, 코드): Claude 단독 OK

---

## 4. 스킬 (슬래시 명령어) 전체 목록

### 세션 관리
| 명령어 | 위치 | 역할 |
|--------|------|------|
| `/pullim` | 글로벌 | 세션 시작. .state/ 로드 + 브리핑 |
| `/handoff` | 글로벌 | 세션 종료. 핸드오프 문서 생성 |
| `/resume-session` | — | 이전 세션 컨텍스트 복원 |

### 작업 관리
| 명령어 | 위치 | 역할 |
|--------|------|------|
| `/harness` | 프로젝트 | 작업 하네스 생성 (목표→작업 분해→트랙 분리→세션 프롬프트) |

### 의사결정/검증
| 명령어 | 위치 | 역할 |
|--------|------|------|
| `/cross-check` | 글로벌 | 교차 모델 검증 (Claude vs GLM-5, 3인칭 프레이밍) |

### 학습
| 명령어 | 위치 | 역할 |
|--------|------|------|
| `/노무사` | — | 노무사 시험 학습 세션 |
| `/anki-gen` | 글로벌 | 학습 내용 → Anki 카드 자동 생성 |
| `/anki-review` | 글로벌 | Claude Code 안에서 Anki 복습 |

### 자기관리
| 명령어 | 위치 | 역할 |
|--------|------|------|
| `/health` | 글로벌 | 건강 데이터 기록 (~/나의_일상/) |
| `/회고` | 글로벌 | 모드1: 감정 기록 (매일) / 모드2: 깊은 회고 (교훈 도출) |
| `/우뇌모드` | 글로벌 | 자유 기록. 흐름과 발산. 분석 안 함 |
| `/night-meeting` | 글로벌 | 밤 회의. Telegram 메시지 수집→분류→안건 처리→저장 |

### 글쓰기
| 명령어 | 위치 | 역할 |
|--------|------|------|
| `/humanizer` | 글로벌 | AI 느낌 제거 (em dash, 과장, 이모지 등 감지→수정) |

### 풀림 제품 테스트
| 명령어 | 위치 | 역할 |
|--------|------|------|
| `/풀림-테스트` | — | 풀림 AI 모드 전환 → 실제 고민 상담 체험 |
| `/풀림-v2-테스트` | — | 멀티 에이전트 버전 체험 |
| `/풀림-강력처방` | — | 분석 없이 행동 처방만 |

---

## 5. Hook (자동 강제 장치)

### 글로벌 (~/.claude/settings.json)
| 이벤트 | 스크립트 | 역할 |
|--------|----------|------|
| PostToolUse (*) | context-auto-save.sh | 컨텍스트 80% 시 자동 transcript 백업 |
| PreCompact (*) | context-auto-save.sh (강제) | compact 전 무조건 백업 |
| Stop | report-usage.js | 사용량 리포트 |

### 프로젝트 (.claude/settings.json)
| 이벤트 | 스크립트 | 역할 |
|--------|----------|------|
| SessionStart | session-start.sh | 자동 컨텍스트 로드 (프로젝트 상태 + 핸드오프) |
| PreToolUse (Edit/Write) | cross-check-guard.sh | 신청서/제품 편집 전 교차검증 알림 |
| PreToolUse (WebSearch/WebFetch) | research-guard.sh | 리서치 단독 진행 **차단** (exit 2, 기술 문서는 통과) |

---

## 6. AI 활용 스택

```
[대표 이상규 = 오케스트레이터]
├── 코딩: Claude Code (Opus=복잡, Sonnet=일상, Haiku=라우팅)
├── 리서치: Gemini Deep Research + Perplexity(GPT 5.4/Kimi/Sonnet) + Claude
├── 의사결정 검증: 교차 모델 (Claude + GLM-5 + GPT 5.4, 최소 2종)
│   ├── 3인칭 프레이밍 필수
│   └── 불일치 지점 = 핵심 의사결정 포인트
├── 콘텐츠/생성: Perplexity+Sonnet / GPT 5.4
└── 품질검증: GLM-5 (Judge Agent) → 불일치 시 3자 판정
```

핵심 원칙: **Claude는 도구 중 하나. 단독으로 결정하지 않음.**

---

## 7. 배치 자동화 (밤샘 작업)

```bash
# 배치 실행
bash scripts/batch_claude.sh

# 모니터링
bash scripts/monitor.sh

# 아침 확인
tail -30 scripts/batch.log
cat scripts/monitor.log
ls scripts/output/*_final.md | wc -l
```

하네스(/harness)로 작업 설계 → scripts/tasks/에 작업 파일 생성 → batch_claude.sh로 실행

---

## 8. Memory (세션 간 학습)

경로: `~/.claude/projects/-Users-sanggyulee-study-main-pullim/memory/`

| 파일 | 내용 |
|------|------|
| project_pullim_restructure.md | 2026-03-18 구조 개편 기록 |
| feedback_research_workflow.md | 리서치 단독 금지 피드백 |
| feedback_agent_quality.md | 에이전트 병렬보다 세션 반복 선호 |
| feedback_overnight_batch.md | 밤샘 배치 자동화 패턴 |

---

## 9. 절대 불변 규칙

1. **위기 안전**: RED 감지 → LLM 즉시 중단 → 전문기관 안내 (109, 1577-0199)
2. **AI 팀 시뮬레이션 금지**: 멀티에이전트 롤플레이 구조적 실패 (ICLR 2025)
3. **덮어쓰기 금지**: decisions/, sessions/, handoffs/ 는 추가만
4. **AI는 augment only**: 절대 replace human judgment 아님
